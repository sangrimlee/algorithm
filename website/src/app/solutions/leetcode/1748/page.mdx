---
title: Sum of Unique Elements
tags:
  - Array
  - Hash Table
  - Counting
---

# 1748. Sum of Unique Elements

<Badges>
  <LevelBadge level="easy" />
  <TopicBadge name="Array" slug="array" />
  <TopicBadge name="Hash Table" slug="hash-table" />
  <TopicBadge name="Counting" slug="counting" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function sumOfUnique(nums: number[]): number {
  const counter = new Map<number, number>();
  for (const num of nums) {
    const count = counter.get(num) ?? 0;
    counter.set(num, count + 1);
  }

  return [...counter.entries()].reduce(
    (prev, [num, count]) => (count === 1 ? prev + num : prev),
    0,
  );
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/sum-of-unique-elements"
  title="Sum of Unique Elements"
/>
