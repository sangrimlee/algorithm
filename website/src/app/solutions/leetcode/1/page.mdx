---
title: Two Sum
tags:
  - Array
  - Hash Table
---

# 1. Two Sum

<Badges>
  <LevelBadge level="easy" />
  <TopicBadge name="Array" slug="array" />
  <TopicBadge name="Hash Table" slug="hash-table" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function twoSum(nums: number[], target: number): number[] {
  const map = new Map<number, number>();
  for (let i = 0; i < nums.length; i++) {
    const currentValue = nums[i];
    const findIndex = map.get(target - currentValue);
    if (findIndex !== undefined) {
      return [findIndex, i];
    } else {
      map.set(currentValue, i);
    }
  }
  throw new Error('There is no solution.');
}
```

<ProblemLink codingSite="leetcode" href="https://leetcode.com/problems/two-sum" title="Two Sum" />
