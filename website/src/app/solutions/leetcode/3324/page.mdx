---
title: Find the Sequence of Strings Appeared on the Screen
tags:
  - String
  - Simulation
---

# 3324. Find the Sequence of Strings Appeared on the Screen

<Badge.Root>
  <Badge.Level level="medium" />
  <Badge.Topic name="String" slug="string" />
  <Badge.Topic name="Simulation" slug="simulation" />
</Badge.Root>

## Solution

```typescript filename="solution.ts" copy
export function stringSequence(target: string): string[] {
  const answer = [];
  let s = '';
  for (const char of target) {
    for (const alphabet of toAlphabet(char)) {
      answer.push(s + alphabet);
    }
    s += char;
  }
  return answer;
}

function* toAlphabet(char: string) {
  const alphabets = 'abcdefghijklmnopqrstuvwxyz';
  for (const alphabet of alphabets) {
    if (char < alphabet) return;
    yield alphabet;
  }
}
```

### Complexity

- Time: $O(n)$
- Space: $O(1)$, 정답에 대한 복잡도 $O(n)$

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/find-the-sequence-of-strings-appeared-on-the-screen"
  title="Find the Sequence of Strings Appeared on the Screen"
/>
