---
title: Maximum Gap
tags:
  - Array
  - Sorting
  - Bucket Sort
  - Radix Sort
---

# 164. Maximum Gap

<Badge.Root>
  <Badge.Level level="medium" />
  <Badge.Topic name="Array" slug="array" />
  <Badge.Topic name="Sorting" slug="sorting" />
  <Badge.Topic name="Bucket Sort" slug="bucket-sort" />
  <Badge.Topic name="Radix Sort" slug="radix-sort" />
</Badge.Root>

## Solution

```typescript filename="solution.ts" copy
export function maximumGap(nums: number[]): number {
  return nums
    .sort((a, b) => a - b)
    .reduce((prev, num, i) => (i === 0 ? prev : Math.max(prev, Math.abs(nums[i - 1] - num))), 0);
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/maximum-gap"
  title="Maximum Gap"
/>
