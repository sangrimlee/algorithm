---
title: Reverse String II
tags:
  - Two Pointers
  - String
---

# 541. Reverse String II

<Badge.Root>
  <Badge.Level level="easy" />
  <Badge.Topic name="Two Pointers" slug="two-pointers" />
  <Badge.Topic name="String" slug="string" />
</Badge.Root>

## Solution

```typescript filename="solution.ts" copy
import { range } from '@algorithm/lib';

export function reverseStr(s: string, k: number): string {
  const reverse = (s: string) => [...s].reverse().join('');

  const n = s.length;
  let answer = '';
  for (const i of range(0, n, 2 * k)) {
    answer += reverse(s.substring(i, i + k)) + s.substring(i + k, i + 2 * k);
  }
  return answer;
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/reverse-string-ii"
  title="Reverse String II"
/>
