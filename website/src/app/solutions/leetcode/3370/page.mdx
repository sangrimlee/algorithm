---
title: Smallest Number With All Set Bits
tags:
  - Math
  - Bit Manipulation
---

# 3370. Smallest Number With All Set Bits

<Badges>
  <LevelBadge level="easy" />
  <TopicBadge name="Math" slug="math" />
  <TopicBadge name="Bit Manipulation" slug="bit-manipulation" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function smallestNumber(n: number): number {
  return (1 << bitLength(n)) - 1;
}

function bitLength(num: number): number {
  let length = 0;
  while (0 < num) {
    num >>= 1;
    length += 1;
  }
  return length;
}
```

### Complexity

- Time: $O(logn)$
- Space: $O(1)$

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/smallest-number-with-all-set-bits"
  title="Smallest Number With All Set Bits"
/>
