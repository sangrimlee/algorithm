---
title: Minimum Number Game
tags:
  - Array
  - Sorting
  - Heap (Priority Queue)
  - Simulation
---

# 2974. Minimum Number Game

<Badges>
  <LevelBadge level="easy" />
  <TopicBadge name="Array" slug="array" />
  <TopicBadge name="Sorting" slug="sorting" />
  <TopicBadge name="Heap (Priority Queue)" slug="heap-priority-queue" />
  <TopicBadge name="Simulation" slug="simulation" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function numberGame(nums: number[]): number[] {
  nums.sort((a, b) => a - b);
  const answer: number[] = [];
  for (let i = 0; i < nums.length; i += 2) {
    answer.push(nums[i + 1]);
    answer.push(nums[i]);
  }
  return answer;
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/minimum-number-game"
  title="Minimum Number Game"
/>
