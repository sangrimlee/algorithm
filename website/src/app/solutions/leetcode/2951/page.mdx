---
title: Find the Peaks
tags:
  - Array
  - Enumeration
---

# 2951. Find the Peaks

<Badge.Root>
  <Badge.Level level="easy" />
  <Badge.Topic name="Array" slug="array" />
  <Badge.Topic name="Enumeration" slug="enumeration" />
</Badge.Root>

## Solution

```typescript filename="solution.ts" copy
export function findPeaks(mountain: number[]): number[] {
  const n = mountain.length;

  const peaks: number[] = [];
  for (let i = 1; i < n - 1; i++) {
    if (mountain[i - 1] < mountain[i] && mountain[i] > mountain[i + 1]) {
      peaks.push(i);
    }
  }
  return peaks;
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/find-the-peaks"
  title="Find the Peaks"
/>
