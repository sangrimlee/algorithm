---
title: Type of Triangle
tags:
  - Array
  - Math
  - Sorting
---

# 3024. Type of Triangle

<Badge.Root>
  <Badge.Level level="easy" />
  <Badge.Topic name="Array" slug="array" />
  <Badge.Topic name="Math" slug="math" />
  <Badge.Topic name="Sorting" slug="sorting" />
</Badge.Root>

## Solution

```typescript filename="solution.ts" copy
export function triangleType(nums: number[]): string {
  const [a, b, c] = nums.sort((a, b) => a - b);
  const type = a + b > c ? new Set(nums).size : -1;
  switch (type) {
    case 1:
      return 'equilateral';
    case 2:
      return 'isosceles';
    case 3:
      return 'scalene';
    default:
      return 'none';
  }
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/type-of-triangle"
  title="Type of Triangle"
/>
