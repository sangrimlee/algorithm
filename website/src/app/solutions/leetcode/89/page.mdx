---
title: Gray Code
tags:
  - Math
  - Backtracking
  - Bit Manipulation
---

# 89. Gray Code

<Badges>
  <LevelBadge level="medium" />
  <TopicBadge name="Math" slug="math" />
  <TopicBadge name="Backtracking" slug="backtracking" />
  <TopicBadge name="Bit Manipulation" slug="bit-manipulation" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function grayCode(n: number): number[] {
  return new Array(1 << n).fill(0).map((_, i) => i ^ (i >> 1));
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/gray-code"
  title="Gray Code"
/>
