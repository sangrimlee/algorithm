---
title: String Compression III
tags:
  - String
---

# 3163. String Compression III

<Badges>
  <LevelBadge level="medium" />
  <TopicBadge name="String" slug="string" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function compressedString(word: string): string {
  let answer = '';
  let [prev, prevCount] = [word[0], 1];
  for (let i = 1; i < word.length; i++) {
    const char = word[i];
    if (prev === char && prevCount < 9) {
      prevCount += 1;
    } else {
      answer += `${prevCount}${prev}`;
      [prev, prevCount] = [char, 1];
    }
  }
  answer += `${prevCount}${prev}`;
  return answer;
}
```

### Complexity

- Time: $O(n)$
- Space: $O(1)$

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/string-compression-iii"
  title="String Compression III"
/>
