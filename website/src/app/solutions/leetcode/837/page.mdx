---
title: New 21 Game
tags:
  - Math
  - Dynamic Programming
  - Sliding Window
  - Probability and Statistics
---

# 837. New 21 Game

<Badge.Root>
  <Badge.Level level="medium" />
  <Badge.Topic name="Math" slug="math" />
  <Badge.Topic name="Dynamic Programming" slug="dynamic-programming" />
  <Badge.Topic name="Sliding Window" slug="sliding-window" />
  <Badge.Topic name="Probability and Statistics" slug="probability-and-statistics" />
</Badge.Root>

## Solution

```typescript filename="solution.ts" copy
import { range } from '@algorithm/lib';

export function new21Game(n: number, k: number, maxPts: number): number {
  const dp = new Array(n + 1).fill(0);
  dp[0] = 1;

  for (const i of range(1, n + 1)) {
    for (const point of range(1, maxPts + 1)) {
      if (point <= i && i - point < k) {
        dp[i] += dp[i - point] / maxPts;
      }
    }
  }

  return dp.slice(k).reduce((acc, curr) => acc + curr, 0);
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/new-21-game"
  title="New 21 Game"
/>
