---
title: Max Consecutive Ones
tags:
  - Array
---

# 485. Max Consecutive Ones

<Badges>
  <LevelBadge level="easy" />
  <TopicBadge name="Array" slug="array" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function findMaxConsecutiveOnes(nums: number[]): number {
  let lastZero = -1;

  let answer = 0;
  nums.forEach((num, i) => {
    if (num === 1) {
      answer = Math.max(answer, i - lastZero);
    } else {
      lastZero = i;
    }
  });
  return answer;
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/max-consecutive-ones"
  title="Max Consecutive Ones"
/>
