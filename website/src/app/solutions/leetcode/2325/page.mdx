---
title: Decode the Message
tags:
  - Hash Table
  - String
---

# 2325. Decode the Message

<Badge.Root>
  <Badge.Level level="easy" />
  <Badge.Topic name="Hash Table" slug="hash-table" />
  <Badge.Topic name="String" slug="string" />
</Badge.Root>

## Solution

```typescript filename="solution.ts" copy
export function decodeMessage(key: string, message: string): string {
  const diagram = new Map<string, string>([[' ', ' ']]);
  const alphabets = 'abcdefghijklmnopqrstuvwxyz';

  let currentIndex = 0;
  for (const char of key) {
    if (!diagram.has(char)) {
      diagram.set(char, alphabets[currentIndex]);
      currentIndex += 1;
    }
  }

  let answer = '';
  for (const char of message) {
    answer += diagram.get(char)!;
  }
  return answer;
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/decode-the-message"
  title="Decode the Message"
/>
