---
title: Sort the People
tags:
  - Array
  - Hash Table
  - String
  - Sorting
---

# 2418. Sort the People

<Badges>
  <LevelBadge level="easy" />
  <TopicBadge name="Array" slug="array" />
  <TopicBadge name="Hash Table" slug="hash-table" />
  <TopicBadge name="String" slug="string" />
  <TopicBadge name="Sorting" slug="sorting" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function sortPeople(names: string[], heights: number[]): string[] {
  const n = names.length;
  const map = new Map<number, string>();
  for (let i = 0; i < n; i++) {
    map.set(heights[i], names[i]);
  }

  return [...map.entries()].sort(([a], [b]) => b - a).map((entry) => entry[1]);
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/sort-the-people"
  title="Sort the People"
/>
