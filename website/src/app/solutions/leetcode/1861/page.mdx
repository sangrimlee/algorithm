---
title: Rotating the Box
tags:
  - Array
  - Two Pointers
  - Matrix
---

# 1861. Rotating the Box

<Badges>
  <LevelBadge level="medium" />
  <TopicBadge name="Array" slug="array" />
  <TopicBadge name="Two Pointers" slug="two-pointers" />
  <TopicBadge name="Matrix" slug="matrix" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function rotateTheBox(box: string[][]): string[][] {
  const [m, n] = [box.length, box[0].length];
  const answer = Array.from({ length: n }, () => new Array<string>(m).fill('.'));
  for (let y = 0; y < m; y++) {
    let lastPosition = n - 1;
    for (let x = n - 1; 0 <= x; x--) {
      if (box[y][x] === '.') continue;
      if (box[y][x] === '#') {
        answer[lastPosition][m - y - 1] = '#';
        lastPosition -= 1;
      } else {
        answer[x][m - y - 1] = '*';
        lastPosition = x - 1;
      }
    }
  }
  return answer;
}
```

### Complexity

- Time: $O(m \cdot n)$
- Space: $O(m \cdot n)$

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/rotating-the-box"
  title="Rotating the Box"
/>
