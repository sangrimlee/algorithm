---
title: Split Strings by Separator
tags:
  - Array
  - String
---

# 2788. Split Strings by Separator

<Badge.Root>
  <Badge.Level level="easy" />
  <Badge.Topic name="Array" slug="array" />
  <Badge.Topic name="String" slug="string" />
</Badge.Root>

## Solution

```typescript filename="solution.ts" copy
export function splitWordsBySeparator(words: string[], separator: string): string[] {
  function splitWord(word: string) {
    return word.split(separator).filter((s) => s !== '');
  }
  return words.flatMap(splitWord);
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/split-strings-by-separator"
  title="Split Strings by Separator"
/>
