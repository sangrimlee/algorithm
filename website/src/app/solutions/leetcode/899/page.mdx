---
title: Orderly Queue
tags:
  - Math
  - String
  - Sorting
---

# 899. Orderly Queue

<Badge.Root>
  <Badge.Level level="hard" />
  <Badge.Topic name="Math" slug="math" />
  <Badge.Topic name="String" slug="string" />
  <Badge.Topic name="Sorting" slug="sorting" />
</Badge.Root>

## Solution

```typescript filename="solution.ts" copy
export function orderlyQueue(s: string, k: number): string {
  if (k !== 1) {
    return [...s].sort().join('');
  }

  const n = s.length;
  let answer = s;
  for (let i = 1; i < n; i++) {
    const curr = s.substring(i) + s.substring(0, i);
    if (curr < answer) {
      answer = curr;
    }
  }
  return answer;
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/orderly-queue/"
  title="Orderly Queue"
/>
