---
title: Number of Changing Keys
tags:
  - String
---

# 3019. Number of Changing Keys

<Badges>
  <LevelBadge level="easy" />
  <TopicBadge name="String" slug="string" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function countKeyChanges(s: string): number {
  const str = s.toLowerCase();
  let answer = 0;
  for (let i = 0; i < str.length - 1; i++) {
    if (str[i] !== str[i + 1]) {
      answer += 1;
    }
  }
  return answer;
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/number-of-changing-keys"
  title="Number of Changing Keys"
/>
