---
title: Total Distance Traveled
tags:
  - Math
  - Simulation
---

# 2739. Total Distance Traveled

<Badges>
  <LevelBadge level="easy" />
  <TopicBadge name="Math" slug="math" />
  <TopicBadge name="Simulation" slug="simulation" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function distanceTraveled(mainTank: number, additionalTank: number): number {
  let answer = 0;
  for (let i = 1; i <= mainTank; i++) {
    if (additionalTank !== 0 && i % 5 === 0) {
      additionalTank -= 1;
      mainTank += 1;
    }
    answer += 10;
  }
  return answer;
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/total-distance-traveled"
  title="Total Distance Traveled"
/>
