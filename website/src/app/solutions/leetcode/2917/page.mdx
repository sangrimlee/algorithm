---
title: Find the K-or of an Array
tags:
  - Array
  - Bit Manipulation
---

# 2917. Find the K-or of an Array

<Badge.Root>
  <Badge.Level level="easy" />
  <Badge.Topic name="Array" slug="array" />
  <Badge.Topic name="Bit Manipulation" slug="bit-manipulation" />
</Badge.Root>

## Solution

```typescript filename="solution.ts" copy
export function findKOr(nums: number[], k: number): number {
  let answer = 0;
  for (let n = 0; n < 31; n++) {
    let count = 0;
    for (const num of nums) {
      if (num & (1 << n)) {
        count += 1;
      }
    }
    if (k <= count) {
      answer += 1 << n;
    }
  }
  return answer;
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/find-the-k-or-of-an-array"
  title="Find the K-or of an Array"
/>
