---
title: Decode XORed Array
tags:
  - Array
  - Bit Manipulation
---

# 1720. Decode XORed Array

<Badges>
  <LevelBadge level="easy" />
  <TopicBadge name="Array" slug="array" />
  <TopicBadge name="Bit Manipulation" slug="bit-manipulation" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function decode(encoded: number[], first: number): number[] {
  const arr = [first];
  encoded.forEach((num, i) => {
    arr.push(num ^ arr[i]);
  });
  return arr;
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/decode-xored-array"
  title="Decode XORed Array"
/>
