---
title: Number of Common Factors
tags:
  - Math
  - Enumeration
  - Number Theory
---

# 2427. Number of Common Factors

<Badge.Root>
  <Badge.Level level="easy" />
  <Badge.Topic name="Math" slug="math" />
  <Badge.Topic name="Enumeration" slug="enumeration" />
  <Badge.Topic name="Number Theory" slug="number-theory" />
</Badge.Root>

## Solution

```typescript filename="solution.ts" copy
export function commonFactors(a: number, b: number): number {
  function gcd(a: number, b: number): number {
    if (a === 0) {
      return b;
    }
    return gcd(b % a, a);
  }

  let answer = 1;
  const maxNum = gcd(a, b);
  for (let num = 2; num <= maxNum; num++) {
    if (a % num === 0 && b % num === 0) {
      answer += 1;
    }
  }

  return answer;
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/number-of-common-factors"
  title="Number of Common Factors"
/>
