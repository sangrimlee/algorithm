---
title: Harshad Number
tags:
  - Math
---

# 3099. Harshad Number

<Badge.Root>
  <Badge.Level level="easy" />
  <Badge.Topic name="Math" slug="math" />
</Badge.Root>

## Solution

```typescript filename="solution.ts" copy
export function sumOfTheDigitsOfHarshadNumber(x: number): number {
  const sum = sumDigits(x);
  return x % sum === 0 ? sum : -1;
}

function sumDigits(num: number) {
  let sum = 0;
  while (0 < num) {
    sum += num % 10;
    num = Math.floor(num / 10);
  }
  return sum;
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/harshad-number"
  title="Harshad Number"
/>
