---
title: Sum of Digits in Base K
tags:
  - Math
---

# 1837. Sum of Digits in Base K

<Badges>
  <LevelBadge level="easy" />
  <TopicBadge name="Math" slug="math" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function sumBase(n: number, k: number): number {
  let answer = 0;
  let current = n;
  while (0 < current) {
    answer += current % k;
    current = Math.floor(current / k);
  }
  return answer;
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/sum-of-digits-in-base-k"
  title="Sum of Digits in Base K"
/>
