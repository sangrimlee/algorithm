---
title: Height Checker
tags:
  - Array
  - Sorting
  - Counting Sort
---

# 1051. Height Checker

<Badges>
  <LevelBadge level="easy" />
  <TopicBadge name="Array" slug="array" />
  <TopicBadge name="Sorting" slug="sorting" />
  <TopicBadge name="Counting Sort" slug="counting-sort" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function heightChecker(heights: number[]): number {
  const expected = [...heights].sort((a, b) => a - b);
  return heights.filter((height, i) => height !== expected[i]).length;
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/height-checker"
  title="Height Checker"
/>
