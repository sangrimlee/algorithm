---
title: Pass the Pillow
tags:
  - Math
  - Simulation
---

# 2582. Pass the Pillow

<Badges>
  <LevelBadge level="easy" />
  <TopicBadge name="Math" slug="math" />
  <TopicBadge name="Simulation" slug="simulation" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function passThePillow(n: number, time: number): number {
  const remain = time % (2 * (n - 1));
  return n - Math.abs(n - 1 - remain);
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/pass-the-pillow"
  title="Pass the Pillow"
/>
