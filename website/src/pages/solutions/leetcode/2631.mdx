---
title: Group By
tags:
---

# 2631. Group By

<Badges>
<LevelBadge level="medium" />

</Badges>

## Solution

```typescript filename="solution.ts" copy
declare global {
  interface Array<T> {
    groupBy(fn: (item: T) => string): Record<string, T[]>;
  }
}

Array.prototype.groupBy = function (fn) {
  return this.reduce((group, item) => {
    const key = fn(item);
    if (group[key]) {
      group[key].push(item);
    } else {
      group[key] = [item];
    }
    return group;
  }, {});
};

export {};
```

<ProblemLink codingSite="leetcode" href="https://leetcode.com/problems/group-by" title="Group By" />
