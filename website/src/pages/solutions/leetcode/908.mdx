---
title: Smallest Range I
tags:
  - Array
  - Math
---

# 908. Smallest Range I

<Badges>
  <LevelBadge level="easy" />
  <TopicBadge name="Array" slug="array" />
  <TopicBadge name="Math" slug="math" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function smallestRangeI(nums: number[], k: number): number {
  const [max, min] = nums.reduce(
    (prev, num) => [Math.max(prev[0], num), Math.min(prev[1], num)],
    [nums[0], nums[0]],
  );
  return Math.max(0, max - min - 2 * k);
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/smallest-range-i"
  title="Smallest Range I"
/>
