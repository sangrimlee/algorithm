---
title: -bit and 2-bit Characters
tags:
  - Array
---

# 717. -bit and 2-bit Characters

<Badges>
  <LevelBadge level="easy" />
  <TopicBadge name="Array" slug="array" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function isOneBitCharacter(bits: number[]): boolean {
  const n = bits.length;
  let currentIndex = 0;
  while (currentIndex < n) {
    if (currentIndex === n - 1) {
      return true;
    }
    currentIndex += bits[currentIndex] === 1 ? 2 : 1;
  }
  return false;
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/1-bit-and-2-bit-characters"
  title="-bit and 2-bit Characters"
/>
