---
title: Clear Digits
tags:
  - Hash Table
  - String
  - Simulation
---

# 3174. Clear Digits

<Badges>
  <LevelBadge level="easy" />
  <TopicBadge name="Hash Table" slug="hash-table" />
  <TopicBadge name="String" slug="string" />
  <TopicBadge name="Simulation" slug="simulation" />
</Badges>

## Solution

```typescript filename="solution.ts" copy
export function clearDigits(s: string): string {
  const stack: string[] = [];
  for (const char of s) {
    if (/\d/.test(char)) {
      stack.pop();
    } else {
      stack.push(char);
    }
  }

  return stack.join('');
}
```

<ProblemLink
  codingSite="leetcode"
  href="https://leetcode.com/problems/clear-digits"
  title="Clear Digits"
/>
