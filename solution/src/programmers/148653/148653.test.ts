import { magicalElevator } from './148653';

describe('LeetCode 148653', () => {
  test('Example 1', () => {
    expect(magicalElevator(16)).toBe(6);
  });
  test('Example 2', () => {
    expect(magicalElevator(2554)).toBe(16);
  });
});
