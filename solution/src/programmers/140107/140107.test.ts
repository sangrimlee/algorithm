import { markingDot } from './140107';

describe('LeetCode 140107', () => {
  test('Example 1', () => {
    expect(markingDot(2, 4)).toBe(6);
  });
  test('Example 2', () => {
    expect(markingDot(1, 5)).toBe(26);
  });
});
