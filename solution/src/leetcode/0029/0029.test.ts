import { divide } from './0029';

describe('LeetCode 0029', () => {
  test('Example 1', () => {
    expect(divide(10, 3)).toBe(3);
  });
  test('Example 2', () => {
    expect(divide(7, -3)).toBe(-2);
  });
});
