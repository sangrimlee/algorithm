import { sumOfMultiples } from './2652';

describe('LeetCode 2652', () => {
  test('Example 1', () => {
    expect(sumOfMultiples(7)).toBe(21);
  });
  test('Example 2', () => {
    expect(sumOfMultiples(10)).toBe(40);
  });
  test('Example 3', () => {
    expect(sumOfMultiples(9)).toBe(30);
  });
});
