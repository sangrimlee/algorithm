import { smallestEvenMultiple } from './2413';

describe('LeetCode 2413', () => {
  test('Example 1', () => {
    expect(smallestEvenMultiple(5)).toBe(10);
  });
  test('Example 2', () => {
    expect(smallestEvenMultiple(6)).toBe(6);
  });
});
