import { countEven } from './2180';

describe('LeetCode 2180', () => {
  test('Example 1', () => {
    expect(countEven(4)).toBe(2);
  });
  test('Example 2', () => {
    expect(countEven(30)).toBe(14);
  });
});
