import { minimumSum } from './2160';

describe('LeetCode 2160', () => {
  test('Example 1', () => {
    expect(minimumSum(2932)).toBe(52);
  });
  test('Example 2', () => {
    expect(minimumSum(4009)).toBe(13);
  });
});
