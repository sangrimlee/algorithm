import { passThePillow } from './2582';

describe('LeetCode 2582', () => {
  test('Example 1', () => {
    expect(passThePillow(4, 5)).toBe(2);
  });
  test('Example 2', () => {
    expect(passThePillow(3, 2)).toBe(3);
  });
});
