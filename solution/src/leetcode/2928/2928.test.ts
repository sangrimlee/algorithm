import { distributeCandies } from './2928';

describe('LeetCode 2928', () => {
  test('Example 1', () => {
    expect(distributeCandies(5, 2)).toBe(3);
  });
  test('Example 2', () => {
    expect(distributeCandies(3, 3)).toBe(10);
  });
});
