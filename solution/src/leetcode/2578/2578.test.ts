import { splitNum } from './2578';

describe('LeetCode 2578', () => {
  test('Example 1', () => {
    expect(splitNum(4325)).toBe(59);
  });
  test('Example 2', () => {
    expect(splitNum(687)).toBe(75);
  });
});
