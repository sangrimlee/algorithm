import { numberOfMatches } from './1688';

describe('LeetCode 1688', () => {
  test('Example 1', () => {
    expect(numberOfMatches(7)).toBe(6);
  });
  test('Example 2', () => {
    expect(numberOfMatches(14)).toBe(13);
  });
});
