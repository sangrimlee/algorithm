import { punishmentNumber } from './2698';

describe('LeetCode 2698', () => {
  test('Example 1', () => {
    expect(punishmentNumber(10)).toBe(182);
  });
  test('Example 2', () => {
    expect(punishmentNumber(37)).toBe(1478);
  });
});
