import { distinctIntegers } from './2549';

describe('LeetCode 2549', () => {
  test('Example 1', () => {
    expect(distinctIntegers(5)).toBe(4);
  });
  test('Example 2', () => {
    expect(distinctIntegers(3)).toBe(2);
  });
});
