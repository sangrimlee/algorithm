import { kthCharacter } from './3304';

describe('LeetCode 3304', () => {
  test('Example 1', () => {
    expect(kthCharacter(5)).toBe('b');
  });
  test('Example 2', () => {
    expect(kthCharacter(10)).toBe('c');
  });
});
