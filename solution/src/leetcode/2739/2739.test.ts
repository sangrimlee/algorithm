import { distanceTraveled } from './2739';

describe('LeetCode 2739', () => {
  test('Example 1', () => {
    expect(distanceTraveled(5, 10)).toBe(60);
  });
  test('Example 2', () => {
    expect(distanceTraveled(1, 2)).toBe(10);
  });
});
