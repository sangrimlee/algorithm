import { findComplement } from './0476';

describe('LeetCode 0476', () => {
  test('Example 1', () => {
    expect(findComplement(5)).toBe(2);
  });
  test('Example 2', () => {
    expect(findComplement(1)).toBe(0);
  });
});
