import { integerBreak } from './0343';

describe('LeetCode 0343', () => {
  test('Example 1', () => {
    expect(integerBreak(2)).toBe(1);
  });
  test('Example 2', () => {
    expect(integerBreak(10)).toBe(36);
  });
});
