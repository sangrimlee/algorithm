import { numberOfCuts } from './2481';

describe('LeetCode 2481', () => {
  test('Example 1', () => {
    expect(numberOfCuts(4)).toBe(2);
  });
  test('Example 2', () => {
    expect(numberOfCuts(3)).toBe(3);
  });
});
