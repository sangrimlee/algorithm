import { countTriples } from './1925';

describe('LeetCode 1925', () => {
  test('Example 1', () => {
    expect(countTriples(5)).toBe(2);
  });
  test('Example 2', () => {
    expect(countTriples(10)).toBe(4);
  });
});
