import { multiply } from './0043';

describe('LeetCode 0043', () => {
  test('Example 1', () => {
    expect(multiply('2', '3')).toBe('6');
  });
  test('Example 2', () => {
    expect(multiply('123', '456')).toBe('56088');
  });
});
