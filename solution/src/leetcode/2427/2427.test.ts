import { commonFactors } from './2427';

describe('LeetCode 2427', () => {
  test('Example 1', () => {
    expect(commonFactors(12, 6)).toBe(4);
  });
  test('Example 2', () => {
    expect(commonFactors(25, 30)).toBe(2);
  });
});
