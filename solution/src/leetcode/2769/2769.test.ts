import { theMaximumAchievableX } from './2769';

describe('LeetCode 2769', () => {
  test('Example 1', () => {
    expect(theMaximumAchievableX(4, 1)).toBe(6);
  });
  test('Example 2', () => {
    expect(theMaximumAchievableX(3, 2)).toBe(7);
  });
});
