import { sum } from './leetcode-2235';

describe('2024-02-15: LeetCode 2235', () => {
  test('Example 1', () => {
    expect(sum(12, 5)).toBe(17);
  });
  test('Example 2', () => {
    expect(sum(-10, 4)).toBe(-6);
  });
});
