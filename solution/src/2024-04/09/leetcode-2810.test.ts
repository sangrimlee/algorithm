import { finalString } from './leetcode-2810';

describe('2024-04-09: LeetCode 2810', () => {
  test('Example 1', () => {
    expect(finalString('string')).toBe('rtsng');
  });
  test('Example 2', () => {
    expect(finalString('poiinter')).toBe('ponter');
  });
});
