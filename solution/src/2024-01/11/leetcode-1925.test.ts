import { countTriples } from './leetcode-1925';

describe('2024-01-11: LeetCode 1925', () => {
  test('Example 1', () => {
    expect(countTriples(5)).toBe(2);
  });
  test('Example 2', () => {
    expect(countTriples(10)).toBe(4);
  });
});
