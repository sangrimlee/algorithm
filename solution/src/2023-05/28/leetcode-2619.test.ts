import './leetcode-2619';

describe('2023-05-28: LeetCode 2619', () => {
  test('Example 1', () => {
    expect([1, 2, 3].last()).toEqual(3);
  });
  test('Example 2', () => {
    expect([].last()).toEqual(-1);
  });
});
