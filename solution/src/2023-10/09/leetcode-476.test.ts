import { findComplement } from './leetcode-476';

describe('2023-10-09: LeetCode 476', () => {
  test('Example 1', () => {
    expect(findComplement(5)).toBe(2);
  });
  test('Example 2', () => {
    expect(findComplement(1)).toBe(0);
  });
});
