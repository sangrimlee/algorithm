import { integerBreak } from './leetcode-343';

describe('2023-10-06: LeetCode 343', () => {
  test('Example 1', () => {
    expect(integerBreak(2)).toBe(1);
  });
  test('Example 2', () => {
    expect(integerBreak(10)).toBe(36);
  });
});
