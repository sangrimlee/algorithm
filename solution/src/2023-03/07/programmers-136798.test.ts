import { knightlyWeapon } from './programmers-136798';

describe('2023-03-07: Programmers 136798', () => {
  test('Example 1', () => {
    expect(knightlyWeapon(5, 3, 2)).toBe(10);
  });

  test('Example 2', () => {
    expect(knightlyWeapon(5, 3, 2)).toBe(10);
  });
});
